# Required in production. Use a pooled PostgreSQL connection string when the
# provider supplies one.
DATABASE_URL=postgresql://user:password@host:5432/offer_flow?sslmode=require

# Optional local-only escape hatch. Production always requires PostgreSQL.
# DEAL_REPOSITORY=memory

# Optional analytics integration. Disabled by default.
NEXT_PUBLIC_ENABLE_ANALYTICS=false
